import { Grid } from "@mui/material";
import { Box } from "@mui/system";

export default function About(props) {
    return (<Box sx={{height: 200, boxShadow: 3, borderRadius: 2.5}}>Hello there</Box>
    );
}